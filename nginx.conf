server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /signup {
        proxy_pass http://node-app:3000;
    }

    location /login {
        proxy_pass http://node-app:3000;
    }
}